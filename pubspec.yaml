name: unipass_web_sdk
description: A Flutter plugin that allows you using unipass with webview page.
version: 0.0.3
homepage: https://github.com/UniPassID/unipass-flutter-web-sdk

environment:
  sdk: '>=2.18.1 <3.0.0'
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_inappwebview: ^5.4.4+3
  web3dart: ^2.3.5
  http: ^0.13.5
  shared_preferences: ^2.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.2.1

flutter:
